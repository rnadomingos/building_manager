@layout('main')
@section('content')
<h1 class="mb-3">{{ title }}</h1>
<div class="list-group">
  @each(item in aptos )
  <a href="/ap/{{ item.ap }}" class="list-group-item list-group-item-action">{{ item.nome_prop }}</a>    
  @endeach
</div>
@endsection